# Bean

## Setup

```
$ python3 -m virtualenv pythonenv
$ source pythonenv/bin/activate
$ pip install -r requirements.txt
$ # In a new/different location
$ git clone https://github.com/waveform80/picamera.git
$ cd picamera
$ python setup develop # or install
```
